<%= form_with(model: recipe_food) do |form| %>
  <% if recipe_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipe_food.errors.count, "error") %> prohibited this recipe_food from being saved:</h2>

      <ul>
        <% recipe_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>  

  <div>
    <%= form.label :food_id, "Select a food:", style: "display: block" %>
    <%= form.select :food_id, Food.all.map { |food| [food.name, food.id] }, prompt: "Select a food" %>
  </div>
      <%= form.hidden_field :recipe_id, value: params[:recipe_id]%>    

    <div class='mb-3' >
      <%= form.label :quantity, style: "display: block" %>
      <%= form.text_field :quantity %>
    </div>

    <div>
      <%= form.submit %>
    </div>
<% end %>